<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/tools">

    <data>
        <variable
            name="loading"
            type="boolean"/>
        <variable
            name="onClickListener"
            type="android.view.View.OnClickListener"/>
    </data>

    <RelativeLayout
        style="@style/AppTheme.Initial">

        <LinearLayout
            android:id="@+id/lnrLoginHeader"
            android:orientation="vertical"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/login_footer_header_margin"
            android:gravity="center"
            android:layout_centerHorizontal="true">

            <ImageView
                android:id="@+id/imgLoginLogo"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/ic_logo"/>

            <TextView
                style="@style/TextView.Title.Accent"
                android:text="@string/app_name"/>

        </LinearLayout>

        <LinearLayout
            android:id="@+id/lnrLoginContent"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:layout_centerInParent="true"
            android:layout_above="@+id/lnrLoginFooter"
            android:layout_below="@+id/lnrLoginHeader"
            android:gravity="center">

            <EditText
                android:id="@+id/edtLoginUsername"
                style="@style/EditText.FormAccent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/username"
                android:inputType="text"/>

            <EditText
                android:id="@+id/edtLoginPassword"
                style="@style/EditText.FormAccent"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="@string/password"
                android:inputType="textPassword"/>

        </LinearLayout>

        <LinearLayout
            android:id="@+id/lnrLoginFooter"
            android:orientation="vertical"
            android:gravity="center"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_margin="@dimen/login_footer_header_margin">

            <Button
                android:id="@+id/btnLoginSend"
                style="@style/Button.Accent"
                android:text="@string/login_button_send"
                android:onClick="@{onClickListener::onClick}"/>

            <TextView
                android:id="@+id/txtLoginRegister"
                style="@style/TextView.Title.Link"
                android:gravity="center"
                android:text="@string/login_register"
                android:onClick="@{onClickListener::onClick}"/>

        </LinearLayout>

        <include
            layout="@layout/view_loading"
            bind:loading="@{loading}"/>

    </RelativeLayout>

</layout>